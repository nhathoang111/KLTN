# Database Configuration - Using MySQL with Docker
spring.datasource.url=jdbc:mysql://mysql-1f67f030-schoolmanagement11.f.aivencloud.com:14244/defaultdb?ssl-mode=REQUIRED
spring.datasource.username=avnadmin
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Character encoding configuration
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# Logging configuration
logging.level.com.example.schoolmanagement=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Logging Configuration
logging.level.org.springframework.security=DEBUG
logging.level.root=INFO
logging.level.com.example=DEBUG
logging.file.name=app.log

# Server port
server.port=8080

# JPA: bật OSIV để Jackson serialize entity Schedule/User/... sau POST/PUT không lỗi lazy (500 dù DB đã lưu).
# Nếu tắt lại, cần DTO hoặc JOIN FETCH đầy đủ trong ScheduleRepository.findByIdWithRelations.
spring.jpa.open-in-view=true

# Multipart file upload configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.file-size-threshold=2KB